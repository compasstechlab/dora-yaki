steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg'
      - 'VITE_API_BASE=${_VITE_API_BASE}'
      - '--build-arg'
      - 'VITE_DEFAULT_LOCALE=${_VITE_DEFAULT_LOCALE}'
      - '-t'
      - '${_IMAGE}'
      - '.'
images:
  - '${_IMAGE}'
